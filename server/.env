PORT=8080
HOST=0.0.0.0
# OpenAPI Try it out / server URL in spec (no trailing path; paths include /v1/...)
PUBLIC_API_ORIGIN=http://127.0.0.1:8080
# Optional: directory for users.json / sessions.json (default: ./data under server root)
# DATA_DIR=/absolute/path/to/data
