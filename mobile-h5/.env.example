# Dev: Vite proxies /v1 to this URL (see vite.config.ts)
# For the repo server/: use http://127.0.0.1:8080
VITE_DEV_API_TARGET=http://192.168.44.3:8080

# API prefix: use /v1 + proxy in dev; in prod use full URL if static and API differ
VITE_API_PREFIX=/v1
